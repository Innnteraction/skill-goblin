# Agent 작업 규칙

## 기본 언어와 변경 원칙

- 사용자와 문서는 한국어를 기본으로 작성한다. 코드 식별자, Skill 이름, 명령, 검색 키워드는 영어를 사용할 수 있다.
- 요청을 만족하는 최소 범위만 변경하고 관련 없는 사용자 변경을 보존한다.
- 스크립트를 추가하거나 수정하면 성공 경로와 대표 실패 경로를 실제로 실행해 검증한다.
- Skill은 `skills/<skill-name>/SKILL.md`를 공용 원본으로 관리한다. 도구별 설치 경로의 복사본을 직접 편집하지 않는다.
- Skill의 핵심 지침은 간결하게 유지하고 세부 자료는 필요한 경우에만 `references/`, `scripts/`, `assets/`로 분리한다.

## 라이선스

- 외부 코드나 Skill 문구를 반입하기 전에 해당 revision의 라이선스를 확인한다.
- MIT가 확인된 자료만 복사하거나 변형한다. 원본 저작권 고지와 MIT 조건을 보존한다.
- 파생 Skill에는 `references/provenance.md`를 추가하고 `THIRD_PARTY_NOTICES.md`를 갱신한다.
- 라이선스가 없거나 불명확하면 내용을 반입하지 않는다. 아이디어만 참고해도 출처와 판단 근거를 기록한다.

## 공개 저장소 보안

- 개인·회사 계정 정보, 인증서, 토큰, 비밀번호, 회사 기밀, 내부 문서, 고객·직원 개인정보, 실제 업무 데이터를 저장소에 넣지 않는다.
- 커밋 전 `scripts/check-sensitive.ps1 -Staged`와 Skill 검증을 실행한다.
- 자동 검사가 통과해도 staged diff를 사람이 읽고 공개 가능한 정보인지 확인한다.
- secret 의심 항목을 발견하면 값을 출력하거나 복사하지 말고 파일과 위치만 보고한다.
