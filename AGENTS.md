# Agent 작업 규칙

## 구현 원칙

1. 구현 방향을 정하기 전에 저장소의 기존 방식과 같은 문제를 해결한 검증된 제품·도구를 조사하고, 적합한 관례를 우선 사용한다.
2. 쓰이지 않는 경로를 호환 계층·폴백·마이그레이션으로 계속 남기지 말고 제거한다. 단, 공개된 CLI나 외부 인터페이스는 명시적 결정 없이 깨뜨리지 않는다.
3. 현재 요구사항을 온전히 만족하는 가장 단순한 구현을 택하고, 추측성 추상화·설정·간접 계층을 만들지 않는다.
4. 엔드투엔드로 동작하는 최소 단위부터 검증하고, 확인된 결과 위에 기능을 단계적으로 더한다.
5. 컴포넌트와 스크립트는 역할별로 모듈화하고 관심사를 명확히 나눈다.
6. 유지보수되는 검증된 라이브러리가 복잡도를 낮추거나 안정성을 높인다면 흔한 기능을 다시 만들지 말고 활용한다.
7. 직접 구현하거나 패키지를 추가하기 전에 기존 의존성의 문서·타입·실제 제공 기능을 확인한다.
8. 장기간 유지할 수 있는 구조를 선택하고, 나중에 교체할 것을 전제로 한 임시방편을 받아들이지 않는다.

## 저장소 고유 원칙

- 사용자 응답과 문서는 한국어를 기본으로 하며 코드 식별자·명령·검색어에는 영어를 사용할 수 있다.
- 요청 범위만 변경하고 관련 없는 사용자 작업과 기존 변경을 보존한다.
- 변경한 스크립트는 성공 경로와 대표 실패 경로를 실제로 실행해 검증한다.
- `skills/`를 Codex·Claude Code 공용 원본으로 사용하고 설치 경로의 복사본은 직접 수정하지 않는다.
- Skill 본문에는 핵심 절차만 두고 상세 자료·결정적 작업·출력 재료는 각각 `references/`, `scripts/`, `assets/`로 분리한다.
- 외부 코드·Skill 문구는 해당 revision의 MIT 라이선스가 확인된 경우에만 반입하고 필요한 저작권·라이선스 고지를 보존한다. 라이선스가 없거나 불명확하면 반입하지 않는다.
- 개인·회사 인증정보, 기밀, 내부 문서, 개인정보, 실제 업무 데이터를 저장소에 반입하지 않는다.
- 자동 검사가 통과해도 커밋하거나 공개하기 전에 staged diff를 직접 읽고 공개 가능성을 판단한다.
- 비밀값 또는 의심 값을 출력·복사하지 말고 파일, 위치, 탐지 규칙만 보고한다.

## 선택 참조

현재 작업과 일치하는 단계가 있을 때만 [Agent 참조 인덱스](docs/agent/INDEX.md)의 해당 문서를 읽는다. 배경 설명, 전체 명령 사용법, 탐색 가능한 저장소 정보는 이 파일에 추가하지 않는다.
